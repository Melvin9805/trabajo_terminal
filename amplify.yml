version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm cache clean --force
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist/tt-pagina-web
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
