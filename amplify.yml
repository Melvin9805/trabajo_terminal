version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --omit=dev
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist/tt-pagina-web
    files:
      - '**/*'
  cache:
      - npm cache clean --force
  paths:
      - node_modules/**/*
